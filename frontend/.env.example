# API Configuration
VITE_API_BASE_URL=http://localhost:8000/api
Change this URL to point to your backend API. For example, if your backend is running on a different host or port, update the URL accordingly.
